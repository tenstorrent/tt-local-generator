tt-local-generator (0.59.1) noble; urgency=medium

  * fix(library): deterministic gallery|detail split — the start child is now
    shrinkable and the divider is snapped to a fixed position on every section
    switch (_reset_detail_split), so the left column no longer jitters
    narrower/wider depending on click order (content min-width was fighting a
    stale divider position).
  * ui(create): move "Surprise me" out of the possibilities-wall header into the
    bottom CTA button area, beside Create (drives the wall's surprise()).

 -- Taylor Singletary <tsingletary@tenstorrent.com>  Mon, 03 Aug 2026 09:32:11 -0700

